pipeline {
    agent any
    options([
        pipelineTriggers([
            pollSCM('* * * * *')
        ])
    ])


    stages {
        stage('Hello World') {
            steps {
                echo 'Hello, World from feature1 branch!'
                echo 'Adding some commit data'
            }
        }
    }
}
